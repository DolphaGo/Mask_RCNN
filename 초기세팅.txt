국룰의 Mask_RCNN 깃 클론(matterport)
conda create -n "가상환경이름" python==3.7 pip

cd를 이용하여 Mask_RCNN 로컬레포지토리로 이동

requirements 가서
tensorflow >= 1.3 을 지우고
tensorflow-gpu < 2.0 을 추가함

pip install -r requirements.txt

conda list를 입력해서 tensorflow가 깔렸는지 확인



